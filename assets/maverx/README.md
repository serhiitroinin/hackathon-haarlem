# Drop Maverx assets here

Download from the challenge Google Drive and save with these exact names:

| Provided file | Save as |
|---|---|
| Master slides (.pptx) | `master.pptx`  ← **required** |
| Style guide (PDF) | `style-guide.pdf` |
| Example training (.docx) | `example.docx` |

These are git-ignored (client material). After adding `master.pptx`, run
`pnpm inspect:master` and fill `src/lib/maverx/layout-map.ts`. See `../../MAVERX.md`.
